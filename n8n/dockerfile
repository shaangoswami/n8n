FROM n8nio/n8n:latest

USER root

# Optional: Install Python or other OS dependencies
RUN apk add --no-cache python3 py3-pip git

# Optional: Install custom global npm packages (if needed)
# RUN npm install -g some-custom-package

# Switch back to the n8n user for security
USER node

